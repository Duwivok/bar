-- Заготовки, созданные через раздел "Выход продукта из сырья" в Конвертере (напр. Цедра лимона,
-- Пилы лайма) — это техническая связка "сырьё -> продукт", а не рецепт, который нужно листать
-- на вкладке "Рецепты". Помечаем их отдельным флагом, чтобы скрыть из общего списка, но не трогать
-- их работу в составах других рецептов (там они ищутся по id, а не по видимости в списке).
-- Выполняется один раз в Supabase: SQL Editor -> New query -> вставить весь файл -> Run.

alter table recipes add column if not exists is_yield_helper boolean not null default false;
