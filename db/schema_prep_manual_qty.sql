-- Дополнение схемы: ручное переопределение нужного количества заготовки на мероприятие.
-- Выполняется один раз в Supabase: SQL Editor -> New query -> вставить весь файл -> Run.
-- Применяется поверх schema.sql + schema_recipes_block.sql + schema_events_block.sql + schema_buy_ready.sql.

-- Если расчёт требует X г/мл заготовки, но пользователь хочет задать нужный объём вручную —
-- это значение (в единице выхода партии рецепта, recipes.yield_unit) заменяет собой
-- расчётное количество и для "Заготовок", и для пересчёта её же состава в "Закупке".
-- NULL — используется расчётное значение как обычно.
alter table event_prep_state add column if not exists manual_qty numeric;
